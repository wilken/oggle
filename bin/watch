#!/usr/bin/env ruby
require 'sinatra'
require 'watch'
require 'thor'

class CLI < Thor
  desc "start", "Start the watch server."
  option :port
  def start
  	set :port, options[:port] if options[:port]
	enable :run 
  end
end
CLI.start(ARGV)
