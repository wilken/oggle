#!/usr/bin/env ruby
require 'sinatra'
require 'watch'
require 'thor'

class CLI < Thor
  desc "start", "Start the watch server."
  option :port
  def start(filename=nil)
  	set :port, option[:port] if option[:port]
	enable :run 
  end
end
CLI.start(ARGV)
