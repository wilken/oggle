#!/usr/bin/env ruby
require 'sinatra'
require 'watch'
require 'thor'

class CLI < Thor
  desc "start", "Start the watch server."
  def start(port)
  	set :port, port if port
	enable :run 
  end
end
CLI.start(ARGV)
